# Copy to .env.local (gitignored) if you need to override the API URL —
# e.g. pointing at a deployed backend instead of the local dev proxy.
# Leave unset for local dev: vite.config.js proxies /api to https://localhost:5001.
# VITE_API_URL=https://localhost:5001